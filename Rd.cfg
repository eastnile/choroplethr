% Rd.cfg  — loaded after Rd.sty
% Make example/code blocks smaller
\AtBeginDocument{%
  \renewcommand{\Examplefont}{\footnotesize\ttfamily} % try \scriptsize if needed
  \renewcommand{\Code}{\ttfamily\footnotesize}
}

% Optional: slightly narrower mono font (if installed)
% \usepackage[scaled=0.85]{inconsolata}